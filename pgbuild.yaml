---
version: 1.0

unit_tests: []

build:
- echo "No build required. Everything is in Dockerfile"

api_gateway:
- service:
      name: helloworld
      protocol: http
  endpoints:
    - path: /
      hosts:
        internal:
        - helloworld
      methods: [GET]
      access:
        public: false
        anonymous: true
